import { Meta } from '@storybook/addon-docs/blocks';
import { Pill, KeyTable, WCAGTable } from '../../../docs/shared/DocBlocks.mdx';

<Meta title="Components/Switch/Accessibility" />

<Pill />

# Accessibility

## Role and ARIA

Switch should use `role="switch"` with `aria-checked` reflecting its on/off state:

- `aria-checked="true"` when the switch is on.
- `aria-checked="false"` when the switch is off.
- `aria-disabled="true"` when `disabled` is `true`.
- `tabindex="0"` when enabled; `tabindex="-1"` when disabled.

Pair the switch with a visible text label. If no visible label is possible, supply `aria-label` or `aria-labelledby` on the host element. The label should describe the feature being toggled, not just the current state (e.g. "Email notifications", not "On").

## Keyboard navigation

<KeyTable rows={[
  { key: 'Tab', behavior: 'Moves focus to the switch (skipped when disabled).' },
  { key: 'Space', behavior: 'Toggles the switch between on and off.' },
]} />

## WCAG compliance

<WCAGTable rows={[
  { criterion: '1.3.1 Info and Relationships', level: 'A', notes: 'role="switch" with aria-checked communicates state programmatically.' },
  { criterion: '1.4.1 Use of Colour', level: 'A', notes: 'On/off state is conveyed by position and colour — supplement with text or aria-checked for users who cannot distinguish colour.' },
  { criterion: '1.4.3 Contrast', level: 'AA', notes: 'Thumb and track meet minimum contrast ratios in both on and off states.' },
  { criterion: '2.1.1 Keyboard', level: 'A', notes: 'Togglable via Space.' },
  { criterion: '2.4.7 Focus Visible', level: 'AA', notes: 'A visible focus indicator is shown on keyboard focus.' },
  { criterion: '3.3.2 Labels or Instructions', level: 'A', notes: 'Always provide a descriptive label so users understand what the switch controls.' },
  { criterion: '4.1.2 Name, Role, Value', level: 'AA', notes: 'Use role="switch", aria-checked, aria-disabled; provide an accessible name via label or aria-label.' },
]} />
