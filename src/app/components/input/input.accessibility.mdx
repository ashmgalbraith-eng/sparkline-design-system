import { Meta } from '@storybook/addon-docs/blocks';
import { Pill, KeyTable, WCAGTable } from '../../../docs/shared/DocBlocks.mdx';

<Meta title="Components/Input/Accessibility" />

<Pill />

# Accessibility

## Role and ARIA

Input renders a native `<input>` element, which carries its own implicit `textbox` role. The component manages label association automatically — the visible `label` is linked to the input via `for`/`id`, so screen readers announce the label when the field receives focus.

- `aria-describedby` should reference `helperText` or `errorText` elements so contextual messages are read after the field label. Wire this up at the application level when embedding the component.
- When `state` is `'error'`, set `aria-invalid="true"` on the host or native input so screen readers announce the error condition.
- Icons inside the field are decorative; they carry `aria-hidden="true"` and do not affect the accessible name.

## Keyboard navigation

<KeyTable rows={[
  { key: 'Tab', behavior: 'Moves focus to the input (skipped when state is \'disabled\').' },
  { key: 'Standard text keys', behavior: 'Type, delete, and navigate within the field as per browser defaults.' },
]} />

## WCAG compliance

<WCAGTable rows={[
  { criterion: '1.3.1 Info and Relationships', level: 'A', notes: 'Label is programmatically associated with the input; helper/error text should use aria-describedby.' },
  { criterion: '1.4.3 Contrast', level: 'AA', notes: 'Label text, input text, and border colours meet minimum contrast.' },
  { criterion: '2.1.1 Keyboard', level: 'A', notes: 'Fully operable via keyboard using native input behaviour.' },
  { criterion: '2.4.7 Focus Visible', level: 'AA', notes: 'A visible focus ring is applied to the input when focused.' },
  { criterion: '3.3.1 Error Identification', level: 'A', notes: 'Use errorText to describe errors in text; set aria-invalid="true" when in error state.' },
  { criterion: '3.3.2 Labels or Instructions', level: 'A', notes: 'Always supply a label; use helperText for format hints.' },
  { criterion: '4.1.2 Name, Role, Value', level: 'AA', notes: 'Native <input> semantics handle role and value; ensure label association is correct.' },
]} />
