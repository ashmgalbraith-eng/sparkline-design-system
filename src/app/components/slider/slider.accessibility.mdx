import { Meta } from '@storybook/addon-docs/blocks';
import { Pill, KeyTable, WCAGTable } from '../../../docs/shared/DocBlocks.mdx';

<Meta title="Components/Slider/Accessibility" />

<Pill />

# Accessibility

## Role and ARIA

Slider renders a native `<input type="range">` element, which carries `role="slider"` implicitly and exposes `aria-valuemin`, `aria-valuemax`, and `aria-valuenow` automatically via the browser.

- Always provide a visible label associated with the input (via `<label for="...">`) or pass `aria-label` / `aria-labelledby` so screen readers announce the slider's purpose.
- When the numeric value has a unit (e.g. "px", "%", "dB"), use `aria-valuetext` to provide a human-readable string (e.g. `"50%"`) instead of relying on the raw number.

## Keyboard navigation

<KeyTable rows={[
  { key: 'Tab', behavior: 'Moves focus to the slider (skipped when disabled).' },
  { key: 'Arrow Right / Arrow Up', behavior: 'Increases value by one step.' },
  { key: 'Arrow Left / Arrow Down', behavior: 'Decreases value by one step.' },
  { key: 'Page Up', behavior: 'Increases value by a larger increment (browser default, typically 10% of range).' },
  { key: 'Page Down', behavior: 'Decreases value by a larger increment.' },
  { key: 'Home', behavior: 'Sets value to min.' },
  { key: 'End', behavior: 'Sets value to max.' },
]} />

## WCAG compliance

<WCAGTable rows={[
  { criterion: '1.3.1 Info and Relationships', level: 'A', notes: 'Label must be programmatically associated with the input.' },
  { criterion: '1.4.3 Contrast', level: 'AA', notes: 'Track and thumb meet minimum contrast against the page background.' },
  { criterion: '2.1.1 Keyboard', level: 'A', notes: 'Fully operable via keyboard using native range input behaviour.' },
  { criterion: '2.4.7 Focus Visible', level: 'AA', notes: 'The thumb receives a visible focus indicator when focused via keyboard.' },
  { criterion: '4.1.2 Name, Role, Value', level: 'AA', notes: 'Native <input type="range"> provides role, min, max, and current value; supply a label and aria-valuetext where appropriate.' },
]} />
