import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Design System Governance/Working with the Design System" />

export const Callout = ({ children }) => (
  <div style={{
    borderLeft: '3px solid #783EF9',
    background: '#F1ECFE',
    padding: '16px 20px',
    borderRadius: '0 8px 8px 0',
    margin: '24px 0',
    fontSize: '15px',
    color: '#3D3D3D',
    lineHeight: 1.6,
  }}>
    {children}
  </div>
);

export const PrincipleList = ({ items }) => (
  <ul style={{ listStyle: 'none', padding: 0, margin: '16px 0 32px', display: 'flex', flexDirection: 'column', gap: '10px' }}>
    {items.map((item, i) => (
      <li key={i} style={{
        display: 'flex',
        gap: '12px',
        alignItems: 'flex-start',
        fontSize: '14px',
        color: '#3D3D3D',
        lineHeight: 1.5,
      }}>
        <span style={{
          flexShrink: 0,
          width: '20px',
          height: '20px',
          borderRadius: '50%',
          background: '#F1ECFE',
          color: '#2C00AC',
          fontSize: '12px',
          fontWeight: 700,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          marginTop: '1px',
        }}>{i + 1}</span>
        {item}
      </li>
    ))}
  </ul>
);

export const OwnerCard = ({ team, owns, color }) => (
  <div style={{
    border: '1px solid #D6D6D6',
    borderTop: `3px solid ${color}`,
    borderRadius: '8px',
    padding: '20px',
    background: '#fff',
  }}>
    <p style={{ fontSize: '11px', fontWeight: 700, letterSpacing: '0.08em', textTransform: 'uppercase', color, margin: '0 0 10px' }}>{team}</p>
    <p style={{ fontSize: '14px', color: '#3D3D3D', margin: 0, lineHeight: 1.5 }}>{owns}</p>
  </div>
);

<div style={{ display: 'flex', gap: '8px', marginBottom: '8px' }}>
  <span style={{ fontSize: '11px', fontWeight: 700, letterSpacing: '0.08em', textTransform: 'uppercase', color: '#2C00AC', background: '#F1ECFE', padding: '4px 10px', borderRadius: '999px' }}>Governance</span>
</div>

# Working with the Design System

Governance defines ownership, authority, and process to support Design System velocity and system integrity, without blocking delivery.

## Governance Principles

<PrincipleList items={[
  'Design System evolves independently of product releases',
  'Versioning communicates impact and required action, not just what changed',
  'Consumers choose when to adopt updates',
  'Design System is responsible for releasing system updates, not for driving adoption',
  'Governance is not meant to override Brand decisions or introduce unnecessary process',
]} />

## Who Owns What

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '32px' }}>
  <OwnerCard team="Design System" owns="Tokens, colors, components, versioning, releases" color="#2C00AC" />
  <OwnerCard team="Brand" owns="Brand guidelines, frames, and color systems" color="#783EF9" />
  <OwnerCard team="Product UX" owns="Product-level design needs and formal requests" color="#DB00A1" />
  <OwnerCard team="Engineering" owns="Application implementation and version adoption" color="#006ADB" />
</div>

### Boundaries
<PrincipleList items={[
  'Brand defines direction; Design System may alter to meet accessibility requirements',
  'UX submits requests and collaborates, but does not directly modify Design System',
  'Engineering integrates Design System as provided and cannot override tokens without alignment',
]} />

## Submitting a Request

Design System has final approval on what gets built, when, and how. All other teams engage through defined intake channels.

<Callout>
  Requests must be submitted at minimum <strong>3 weeks</strong> before the need date so Design System can review, refine scope, and prioritize the work.
</Callout>

<PrincipleList items={[
  'Design System reviews, refines scope, and prioritizes all incoming work before it is committed to',
  'Engineering may choose when to adopt a Design System version, but cannot hardcode values when tokens exist or override version classifications',
]} />

For how feedback and approvals are handled once work is underway, see [Communication & Enforcement](?path=/docs/design-system-governance-communication-enforcement--docs).
